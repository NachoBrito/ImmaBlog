<article class="comment">
    <h4>{{ thought.authorName }}</h4>
    <img width="64" src="{{thought.authorAvatarUrl}}?s=64" class="pull-left" />
    {{ thought.contentHTML | raw }}
    <div class="clear">
        <p class='muted'>
            {{entry.created|date()}}
        </p>
        
        {# 
        <div>
            <a href="#" class='btn-link activate-comments'>
                <i class="fa fa-reply-all"></i>
                <span>{{'immablog.thought.comments.reply'|trans  }}</span>
            </a>
        </div>
        #}
    </div>
</article>